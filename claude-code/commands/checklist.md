---
name: checklist
description: Generate a well-structured markdown checklist of all tasks required for an implementation. Groups related tasks under section headings (Backend, Frontend, Database, etc.) with clear, actionable `- [ ]` items. Creates a comprehensive, self-contained progress tracking document. Shows planned output for approval before finalizing.
---

Generate a well-structured markdown file listing all tasks required for this implementation.  
Each task should be written as a checklist item (`- [ ]`) with clear, actionable descriptions.  
Group related tasks under appropriate section headings using markdown titles (e.g., `## Backend`, `## Frontend`, `## Database`).  
Ensure the file is comprehensive and can serve as a progress tracking document during development.  
The output should be valid markdown and fully self-contained so it can be directly copied into a project.

Before finalizing, show me the planned output for approval.
