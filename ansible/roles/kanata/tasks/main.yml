---
- name: Check if kanata is installed
  ansible.builtin.stat:
    path: "{{ ansible_facts.env.HOME }}/.cargo/bin/kanata"
  register: kanata_bin

- name: Install kanata via cargo
  ansible.builtin.shell: |
    "{{ ansible_facts.env.HOME }}/.cargo/bin/cargo" install kanata
  args:
    executable: /bin/bash
  when: not kanata_bin.stat.exists
  changed_when: not kanata_bin.stat.exists

- name: Create uinput group
  # group doesn't actually get created under --check, so the user task
  # below can't add a member to it either - skip both rather than fail
  # trying to join a group that (as far as the real system is concerned)
  # doesn't exist yet.
  ansible.builtin.group:
    name: uinput
    state: present
  become: true
  when: not ansible_check_mode

- name: Add user to uinput group
  ansible.builtin.user:
    name: "{{ ansible_facts.env.USER }}"
    groups: uinput
    append: true
  become: true
  when: not ansible_check_mode

- name: Install udev rule for uinput access
  ansible.builtin.copy:
    dest: /etc/udev/rules.d/99-input.rules
    content: |
      KERNEL=="uinput", MODE="0660", GROUP="uinput", OPTIONS+="static_node=uinput"
    owner: root
    group: root
    mode: "0644"
  become: true
  register: kanata_udev_rule

- name: Reload udev rules
  ansible.builtin.shell: |
    udevadm control --reload-rules
    udevadm trigger
  args:
    executable: /bin/bash
  become: true
  when: kanata_udev_rule.changed

- name: Enable and start kanata systemd user service
  # On a fresh --check run kanata was never actually installed (cargo
  # install is a shell task, which skips under check mode), so there's no
  # kanata.service unit for systemd to resolve - skip rather than fail.
  ansible.builtin.systemd_service:
    name: kanata.service
    scope: user
    enabled: true
    state: started
    daemon_reload: true
  when: not ansible_check_mode
